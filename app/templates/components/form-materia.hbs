<form  {{action 'save' on='submit'}}>

clave {{input type='text' value=materia.clave }}
nombre{{input type='text' value=materia.nombre }}

{{#link-to 'nuevo-grupo' materia.id}} <button class="btn btn-primary">Añadir grupo</button> {{/link-to}}
<hr>
<h2>GRUPOS</h2>
<table class="table table-hover table-sm table-bordered table-info">
    <thead class="thead-dark">
      <tr>
        <th scope="col">Identificador</th>
        <th scope="col">periodo</th>
        <th scope="col"> </th>
      </tr>
    </thead>
    <tbody>
      {{#each materia.grupos as |g|}}
        <tr>
          <th>
            {{g.identificador}}

          </th>
          <td>
            {{g.periodo}}
          </td>
          <td>
            {{#link-to "grupo-detail" g.id}}
              <button class="btn btn-success btn-sm">Detalles</button>
            {{/link-to}} <button class="btn btn-danger btn-sm "{{action 'destroyGrupo' materia g}}>Borrar</button>
          </td>
        </tr>


      {{/each}}
    </tbody>
</table>





<button class="btn btn-info">Guardar</button>
<hr>


</form>
